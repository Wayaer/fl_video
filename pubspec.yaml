name: fl_video
description: For the video_Player control panel, add Cupertino and Material styles
version: 1.4.1
repository: https://github.com/Wayaer/fl_video.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.9.1
  wakelock_plus: ^1.2.7

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
